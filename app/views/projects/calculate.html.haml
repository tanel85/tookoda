%h2 Saasteluba

%table#pollutionPermit
  %thead
    %tr
      %th Kemikaal
      %th Aine nimetus
      %th g/s
      %th t/a
      %th mgC/m3
  %tbody
    - @pollPermRows.each do |pollPermRow|
      %tr
        %td #{pollPermRow.chemical_name}
        %td #{pollPermRow.chemical_element_name}
        %td #{pollPermRow.gs.round(4)}
        %td #{pollPermRow.ta.round(4)}
        %td #{pollPermRow.mgcm3.round(4)}
    %tr
      %td{:colspan => 2, :align => 'right'} 
        %strong Kokku:
      %td 
        %strong #{@gs_sum.round(4)}
      %td 
        %strong #{@ta_sum.round(4)}
      %td 
        %strong #{@mgcm3_sum.round(4)}       
%br
  
= button_to 'Trüki heitkogused', print_project_path(@project_id), :method => :get
= button_to 'Tagasi', project_chemicals_project_path(@project_id), :method => :get